jenkins:
  systemMessage: "Enterprise Jenkins - Environment ACL"
  numExecutors: 4
  
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "sre"
          password: "sre123"

  authorizationStrategy:
    globalMatrix:
      entries:
        - user:
            name: "sre"
            permissions:
              - "Overall/Administer"

