jobs:
  - script: >
      pipelineJob('ansible-job') {
        definition {
          cps {
            script('''@Library('pulsar-library') _
      test_ansible_step()'''.stripIndent())
            sandbox()
          }
        }
      }